# Cross-crate integration + determinism harness (SPEC §4, §14).
# Test-only crate: everything lives under tests/, nothing ships from here.
[package]
name = "embervale-tests"
version.workspace = true
edition.workspace = true
publish.workspace = true

[lib]
# No library code; this package exists to host integration tests.
path = "src/lib.rs"

[dev-dependencies]
core_types = { workspace = true }
core_rng = { workspace = true }
core_ecs = { workspace = true }
sim_time = { workspace = true }
persistence = { workspace = true }
headless = { workspace = true }
serde = { workspace = true }
