# Layer: sim. May depend on: core_* + external crates only; never another
# sim_* crate (SPEC §4) — shared components come from core_ecs::sim_interface,
# and the resolved cross-config tables are built by data_defs and passed in.
[package]
name = "sim_ai"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
core_types = { workspace = true }
core_ecs = { workspace = true }
serde = { workspace = true }
