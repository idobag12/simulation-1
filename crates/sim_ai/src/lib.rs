//! Utility AI, planners, memory, relationships (SPEC §11).
//!
//! Empty shell: built in Phase 3 (SPEC §15). Per SPEC §16.1 nothing is
//! stubbed here — the crate exists only to fix the workspace layout and
//! dependency rules from day one.

#![forbid(unsafe_code)]
#![warn(missing_docs)]
#![warn(clippy::unwrap_used, clippy::expect_used, clippy::panic)]
