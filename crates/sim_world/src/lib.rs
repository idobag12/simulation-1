//! Town map, buildings, housing, abstract pathing (SPEC §15 Phase 6/9).
//!
//! Empty shell: built in Phase 6 (SPEC §15). Per SPEC §16.1 nothing is
//! stubbed here — the crate exists only to fix the workspace layout and
//! dependency rules from day one.

#![forbid(unsafe_code)]
#![warn(missing_docs)]
