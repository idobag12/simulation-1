# Layer: service. May depend on: sim_*, core_* + external crates.
# Built from Phase 4 (ADR 0007: the conservation auditors live here,
# SPEC §13). New external dependencies require an ADR (SPEC §2).
[package]
name = "debug_tools"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
core_types = { workspace = true }
core_ecs = { workspace = true }
sim_people = { workspace = true }
