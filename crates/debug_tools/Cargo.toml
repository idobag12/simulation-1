# Layer: service. May depend on: sim_*, core_* + external crates.
# Empty shell until Phase 2+; dependencies are added in the phase that builds
# it (ADR 0001). New external dependencies require an ADR (SPEC §2).
[package]
name = "debug_tools"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
